<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qnaMapper">
  	
  	<!-- 문의사항 총 개수 -->
	<select id="listCount" resultType="_int">
		SELECT COUNT(*)
		FROM QNA
		WHERE STATUS = 'Y'
	</select>
	
	<!-- 문의사항 목록 조회 -->
	<select id="qnaListView" resultType="Qna">
		SELECT Q_NO, Q_TYPE, Q_TITLE, CONTENT, CREATE_DATE, MEM_ID, P_NO
		FROM QNA Q
		JOIN MEMBER USING (MEM_ID)
		JOIN PARKINGLOT USING(P_NO)
		WHERE Q.STATUS = 'Y'
		ORDER BY Q.CREATE_DATE DESC
	</select>
</mapper>